repos:
  - repo: local
    hooks:
      - id: quality-gates
        name: quality gates (check-ci)
        entry: uv run check-ci
        language: system
        stages: [pre-commit, pre-push]
        pass_filenames: false
