legacy_public_commands = ["format-check"]

# Single source of truth for the gitleaks pin. The pre-commit mirror `rev` and
# the CI install step must both match this value; audit_secret_scanning enforces
# the lockstep so the local and CI secret scans never diverge.
[secret_scanning]
gitleaks_version = "v8.30.1"

[published_entry_points]
activity-report = "oaknational.python_repo_template.demo.activity_report:main"

[documentation_commands]
".agent/directives/AGENT.md" = [
  "clean",
  "build",
  "dev",
  "check",
  "check-ci",
  "fix",
  "test",
  "coverage",
]
".agent/skills/start-right-quick/SKILL.md" = ["check", "check-ci"]
".agent/commands/gates.md" = ["clean", "build", "dev", "check", "check-ci"]
"README.md" = ["clean", "build", "dev", "check", "check-ci", "fix", "test", "coverage"]
"docs/dev-tooling.md" = [
  "clean",
  "build",
  "dev",
  "format",
  "format-fix",
  "lint",
  "lint-fix",
  "markdownlint",
  "markdownlint-fix",
  "typecheck",
  "repo-audit",
  "test",
  "coverage",
  "fix",
  "check",
  "check-ci",
]
