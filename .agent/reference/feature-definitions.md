# Feature Definitions

Use this structure when documenting a validated field or derived value:

- name
- source
- type
- validation rule
- downstream use

For the seeded demo, the canonical fields are `date`, `category`, `minutes`,
and `notes`.
