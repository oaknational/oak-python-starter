# Changelog

## [oaknational-python-repo-template] 2026-04-23 — Full Memory and Feedback Integration

- Expanded the portable Core contract to include `practice-verification.md`,
  `decision-records/`, and `patterns/`.
- Reframed the Practice around the three-plane memory estate:
  `active/`, `operational/`, and `executive/`.
- Added split closeout doctrine: `session-handoff` for session continuity and
  `consolidate-docs` for graduation and incoming Practice integration.
- Tightened the local propagation rule so durable governance and portable
  patterns live in the Core, while `.agent/practice-context/` stays ephemeral.
- Standardised the Python gate API around `format`, `format-fix`, `lint`,
  `lint-fix`, `fix`, `check`, and `check-ci`, with no legacy aliases.
- Added the remaining canonical command surfaces: `clean`, `build`, and `dev`,
  and wired a build probe into the aggregate checks.

## [oaknational-python-repo-template] 2026-04-23 — Planning, Knowledge Tiers, and Governance Transfer

- Installed the full planning architecture: strategic plan index, completed-plan
  index, collection roadmaps, documentation-sync logs, and reusable templates.
- Added explicit knowledge tiers for `research/`, `analysis/`, `reports/`,
  `reference/`, `experience/`, and `docs/explorations/`.
- Added the canonical homing method and the `commit` and `tsdoc` skills.
- Backfilled the remaining approved Practice governance bundle and generic rule
  estate from the source Practice.
- Added `docs/dev-tooling.md` as the Python leading-edge tooling reference.

## [oaknational-python-repo-template] 2026-04-21 — Template Extraction

- Seeded a reusable Python template from infrastructure-first repo assets.
- Added the `activity-report` demo CLI and supporting tests.
- Reframed the Practice surfaces around generic infrastructure and demo work.
