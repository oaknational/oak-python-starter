# Workstreams — Retired Surface

This directory is retained only as doctrine and provenance.

The Python template adopts the richer continuity model with threads and tracks,
but not a live workstream layer. At this repo's current scale, lane state lives
directly in each thread's next-session record.

If a future repo built from this template develops genuine multi-lane threads,
the workstream layer can be reintroduced deliberately.
