# Thread: practice-foundation-upgrade

## Participating Agent Identities

| agent_name | platform | model | session_id_prefix | role | first_session | last_session |
| --- | --- | --- | --- | --- | --- | --- |
| Codex | codex | gpt-5 | unknown | executor | 2026-04-23 | 2026-04-23 |

## Owning Plans

- No dedicated plan yet. This thread is currently driven by the user's direct
  instruction plus the Practice integration workflow.

## Current Objective

- Treat this thread as complete baseline work and require a new explicit thread
  before taking in any further source-Practice scope.

## Current State

- The three-plane memory estate is live.
- Portable Practice governance and verification surfaces are live.
- `session-handoff`, the supporting rules, adapter wrappers, and repo audit
  updates are landed.
- The Python quality-gate API is standardised around `format`, `format-fix`,
  `lint`, `lint-fix`, `fix`, `check`, and `check-ci`.
- Orientation, `experience/`, `analysis/`, `reports/`, curated `reference/`,
  `docs/explorations/`, and the full planning architecture are live.
- The approved missing PDR bundle has landed as generalised local doctrine.
- The generic governance-rule bundle and the selected `commit` / `tsdoc`
  skills are live.
- The root README now states the repo's agentic-engineering safety stance
  plainly, and testing doctrine now uses a standard validation taxonomy with
  explicit data-project validation expectations.
- Deep session-handoff and consolidate-docs closeout has refreshed continuity,
  distilled memory, and experience.
- `uv run check` and `uv run check-ci` pass after the integration.

## Blockers / Low-Confidence Areas

- No open blockers remain inside the approved transfer scope.

## Next Safe Step

- Preserve this thread as complete baseline work. If more source-Practice
  material is desired, open a new explicitly-scoped thread and decide transfer
  versus leave before editing.
