# Never Disable Checks

Never disable type checks, linting, formatting, tests, or git hooks (`--no-verify`). Fix the root cause. The code needs to change, not the check.

See `.agent/directives/principles.md` §Code Quality for the full policy.
