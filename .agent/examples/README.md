# Examples

This repo seeds one small example:

- `activity-report` — a CLI that validates activity logs, writes Parquet, prints
  summaries, and can emit a chart

The example exists to exercise the infrastructure, not to define the repo.
